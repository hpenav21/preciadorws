/* Serial Port configuration */
serial port configuration is done using: 
	sudo java -jar serialPortConfig.jar
	
	program will display available serial ports
	type index value then hit enter
	if no errors occurr then config.properties file will be created including the selected port


